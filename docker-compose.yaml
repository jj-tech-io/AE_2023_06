services:
  jupyter-lab:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - .:/AE_2023_06
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
              

